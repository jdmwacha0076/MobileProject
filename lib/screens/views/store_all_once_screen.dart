import 'package:flutter/material.dart';

class StoreAllOncePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fungulia Stoo'),
      ),
      body: Center(
        child: Text('This is the store all once page!'),
      ),
    );
  }
}
