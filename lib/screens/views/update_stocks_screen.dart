import 'package:flutter/material.dart';

class UpdateStocksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sasisha Item'),
      ),
      body: Center(
        child: Text('This is the update stocks page!'),
      ),
    );
  }
}
