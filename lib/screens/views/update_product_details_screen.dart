import 'package:flutter/material.dart';

class UpdateProductDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Badili au Futa Item'),
      ),
      body: Center(
        child: Text('This is the update product details page!'),
      ),
    );
  }
}
