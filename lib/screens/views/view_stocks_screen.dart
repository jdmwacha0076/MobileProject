import 'package:flutter/material.dart';

class ViewStocksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tazama Item'),
      ),
      body: Center(
        child: Text('This is the view stocks page!'),
      ),
    );
  }
}
